{
  "_": "A pscale JSON block that teaches the pscale block format and BSP navigation function by being an operational example of both — every spindle through this block delivers both the explanation of a concept and a structural demonstration of it. Pure form: underscore, digits 1–9, curly brackets, nothing else. No metadata, no wrapper fields. The structure is the complete specification, sufficient for any reader to navigate, author, or implement tooling for any pscale block.",
  "1": {
    "_": "A pscale block uses exactly three kinds of JSON key: underscore for meaning at the zero position, single digits 1 through 9 for branch positions, and curly brackets for nesting depth — these three conventions are the entire format specification, and any consumer that understands them can navigate any block regardless of its subject matter or complexity.",
    "1": {
      "_": "The underscore key holds what a node IS — its meaning stated as a complete thought, never a heading, title, or label. It occupies the zero position in the tree: when BSP walks a number, digit 0 maps to underscore, making the underscore chain from root to any depth the backbone of every spindle the block can produce.",
      "1": {
        "_": "The root underscore is the single most important node because every spindle passes through it and every reader encounters it first — it must answer 'what is this entire block about?' as a complete orientation after which someone knows the domain, what the block offers, and what kind of content lies deeper.",
        "1": "Every non-root underscore serves a dual function simultaneously: it is a complete thought at its own resolution AND the context prefix for everything deeper in its branch. Read it alone — if meaningless, it is a heading. Read it as a prefix to its deepest descendant — if it fails to orient, it is disconnected. Both are authoring failures that degrade every spindle passing through the node.",
        "2": "The heading trap is the most common failure mode: document-trained instincts treat parent nodes as section headers. The test is simple — read the underscore without its children. If trivially obvious or meaningless without them, it is a heading abbreviating a thought that should be stated in full. The heading is three characters; the thought is two sentences; only the thought earns its place in a spindle."
      },
      "2": "Underscore text must be a substantive statement that stands alone — at minimum a full sentence carrying enough semantic weight to be meaningful without its children. This is non-negotiable because spindles extract underscore text at each level, and a thin or cryptic underscore produces a broken spindle regardless of how rich the content beneath it may be.",
      "3": {
        "_": "The underscore always occupies the zero position, but its semantic relationship to its siblings takes one of four forms determined by content, not structure — only an LLM reading the text can determine which form applies, because the JSON structure is identical in all four cases. The forms are named by sign and position: the zero IS the underscore, the sign indicates whether the block is settled (plus) or mutable (minus), and the position of the zero indicates whether the underscore relates to its own bracket or an adjacent one.",
        "1": "0+ (simple past, deductive) — the underscore describes its own group, naming the set it belongs to. Self-referential. Single Möbius: a member of the set that names the set. The description is true once the bracket is filled — the underscore matches the content. Before completion, a single leading point where the truth is not yet verified. Deductive character: the description is given, the content follows from it. Produces rendition blocks that decompose downward — documents, specifications, spatial mappings. Digits are editorial labels and could be reordered without losing meaning.",
        "2": "+0 (past perfect, inductive) — the underscore summarises the previous completed group. Backward-facing. Double Möbius: backward-facing meaning in a forward-facing position. The content under the prior sibling digit is what gets compressed into this underscore. Inductive character: the pattern of what has been observed is generalised into a summary — it has always been this way, therefore this is the record. Produces living blocks that accumulate forward — histories, journals, logs. When digits 01–09 fill and the block supernests, address 10 holds the summary of entries 01–09, address 20 will hold the summary of 11–19. Variations beyond summary: emergence (something new arising from the group that no single entry contained — irreversible) and simple increment (no special semantic weight, just the next entry in sequence).",
        "3": "0− (future, abductive) — the underscore is an instruction for its own group. Forward-facing within the bracket. The content is incomplete — the underscore states what should fill in around it. Willful intent, directionality, not yet resolved. Abductive character: inferring what must be the case to complete the picture. Produces operational blocks — instructions, recipes, kernels, seeds. Natural voice is the zeroth person imperative. Provisional underscores — evolving descriptions updated as entries arrive, unstable until the group completes — are 0− in partial state: the intention is live, not yet resolved.",
        "4": "−0 (future perfect, backcasting) — the underscore in the current bracket is predicated on a future case: the content of the next bracket. The current bracket is written assuming a future state is true, and that assumed future defines what the current bracket must contain. This is backcasting — a future target giving guidance to the present position. When the next bracket is unfilled: future abduction ('fuduction') — what must the future case be, such that the current position makes sense? When the next bracket is filled: retroduction — what must have been the case here, given that the future is already established? The reasoning runs backward from a known future into an unfinished present."
      },
      "4": "The underscore is the zeroth person — the implied perceiver behind its siblings. Every statement has a speaker; every group has an organiser; every set has a perspective from which it coheres. The underscore occupies that position structurally: it sits at zero among the content it perceives, a sibling of its own perceptions. It never says 'I' (first person), never addresses 'you' (second person), never describes 'it' from outside (third person). It simply states — and the stating implies the stater. This is the zeroth person voice: imperative, situated, perspectival without being personal. The discipline for authoring pscale content follows directly: write in zeroth person. Not 'I parse the input' nor 'you should parse the input' nor 'the system parses the input' — just 'parse the input.' The actor is the underscore. The underscore is inside the block.",
      "5": "When an underscore at any depth is subnested into an object with digit children but no underscore of its own, those children occupy the interior of the zero position — accessible only by walking digit 0 into the underscore and then a digit into its content. Address 1.0.2 walks to branch 1, into its underscore (digit 0), then to that underscore's second child. This is the only structural context where 0 appears as a navigated digit in normal use. Embedded in any block, a zero-position interior acts as a hidden directory — documentation, hooks, or metadata carried inside a node without polluting its content branches. A block of form {\"_\": {\"1\": ..., \"2\": ...}} — root underscore subnested without its own underscore — is a zero-position block: headless, with all content at addresses beginning with 0. It has no internal semantic orientation; the block name provides externally what the underscore ladder provides for every other block.",
      "6": "The underscore's semantic content always lives one pscale level below the digit it describes. In address 123, digit 2 is located at the pscale of its parent level, but the meaning of digit 2 — its underscore — sits at the pscale of its children's level. The BSP walk confirms this: to read what digit 2 means, you enter it and find the underscore inside. The description is inside what it describes, one level down. This is the structural Möbius: the label is a member of the set it labels, carried at the set's interior resolution rather than at its exterior position. The only exception is a terminal string — a digit with no children whose meaning is locked to the digit's own pscale, because no lower level exists."
    },
    "2": {
      "_": "Digits 1 through 9 are branch positions — each level of the tree holds at most nine children. A node without children is a string value; when it needs children, the string moves to the underscore key and children take digit keys — this is the only growth operation at a single node, and it preserves the original content as the branch's summary.",
      "1": "Digit ordering carries meaning that depends on the block's form and purpose: sequential (1 happened before 2, temporal or procedural), labelling (1 names one facet, 2 another, parallel in scope), or arbitrary (freely assigned, reorderable). Most levels use three to seven digits; all nine occupied means the level is at capacity.",
      "2": "Empty digit slots are open positions indicating capacity for growth, not missing content. A level with digits 1, 2, and 3 occupied has six open slots — six directions the block can grow at this depth without any structural change.",
      "3": "The transition from terminal string to underscore-with-children is a phase change — the bud. A terminal string carries its meaning at the digit's own pscale. The moment it opens, that meaning drops one pscale level into the underscore, and the digit becomes a branch with capacity for siblings. The bud is the birth of the Möbius at that node: the twist only activates when there is something to twist around. A digit that has never budded is sealed. A digit that has budded is open — structurally capable of branching, even if no siblings have arrived yet."
    },
    "3": {
      "_": "Growth happens in two directions with fundamentally different structural effects. Subnesting grows inward: a leaf gains children, becoming a branch, with the floor unchanged and only addresses at that branch getting longer. Supernesting grows outward: the entire existing content wraps inside a new underscore level, the floor increments by one, every original address gains a 0 prefix, and a new digit opens at the top.",
      "1": {
        "_": "Supernesting is the response to a full block — when all nine digit slots at the top level are occupied and a new entry must arrive. The entire existing tree wraps into the underscore, a new digit 1 opens for the incoming content, and the floor depth increases by one. What was address 1 becomes 01, what was 7 becomes 07 — content unchanged, position shifted.",
        "1": "Each supernest adds exactly one level to the underscore chain. The chain is an archaeological record of growth: the deepest string is the original root content, each level above was created at a successive supernest. A block with underscore chain depth 3 has supernested twice.",
        "2": "After supernesting, the block is a living block (floor > 1) with content above and below the implicit decimal point. The digits above the floor represent accumulated context — broader-than-root meaning that grew as the block expanded. The digits below the floor decompose into finer detail, just as they did before supernesting."
      },
      "2": "Subnesting is local and common — any leaf can gain children at any time without affecting anything else in the block. Parent nodes, sibling nodes, and distant branches remain untouched. This locality means multiple writers can extend different branches simultaneously without conflict, and the block grows organically at whatever points need more detail.",
      "3": "Rearranging content between flat and nested positions is itself a semantic operation — nesting constrains meaning by making each item's expansion dependent on its parent context, while flattening liberates meaning by restoring each item's full independent polysemy. The same words nested differently produce different blocks, different spindles, and different meaning-spaces. This is not a storage choice; it is a compositional choice with semantic consequences."
    },
    "4": {
      "_": "The floor is the implicit decimal point, discovered by following the underscore chain from root: check root._ — is it a string or an object? If a string, floor is 1. If an object with its own underscore, step in and repeat. Count steps until you hit a string. That count is the floor depth, and it equals the number of times the block has supernested plus one. If an object has no underscore of its own, the chain terminates — that object is a zero-position interior, and the floor is the count of underscore keys traversed to reach it. A pscale address is a single number with at most one decimal point — the decimal marks the floor boundary and nothing else. The number of whole-number digits equals the floor: a floor-3 block uses three-digit whole numbers like 345, 110, 100. Trailing zeros indicate the floor width, not additional walk steps — 100 means digit 1 only, 110 means digits 1 then 1, 345 means digits 3 then 4 then 5. Never use multiple decimal points in an address — 2.2.4.5 is not a pscale number.",
      "1": "A rendition block has floor 1 — root._ is a string, all content decomposes downward from pscale 0, and top-level branches are addressed as whole numbers 1 through 9. Subnested children extend with a decimal: branch 2's first child is 2.1. This block is a rendition block: floor 1, never supernested, with five top-level branches.",
      "2": "A living block has floor greater than 1 — it has supernested at least once and carries content above and below the decimal. Address 23.45 walks four digits through the tree; the decimal position after the second digit means two levels of accumulated context sit above the original root.",
      "3": "The floor makes pscale numbers portable across block growth — a node at a given pscale depth means the same structural thing regardless of how many times the block has supernested. The walk just passes through more accumulated context on the way down in a mature block than in a young one.",
      "4": {
        "_": "Worked example: a spatial block with floor 3. The underscore ladder is three deep, indicating three place values to the left of the decimal point. Addresses are padded with zeros to fill those three places. Beyond three digits, a decimal point appears. The decimal point marks the relative point of interest; in spatial terms, the scale at which a human inhabits a room.",
        "1": "Address 100: pscale +2 — village or neighbourhood.",
        "2": "Address 110: pscale +1 — building.",
        "3": "Address 111: pscale 0 — room (human scale).",
        "4": "Address 111.1: pscale -1 — detail within a room (a table, a hearth).",
        "5": "The walk function is identical for any floor. The pscale derived from a given depth changes — that is what the floor does: it calibrates which depth is human scale."
      },
      "5": "An operational block is dominated by 0− underscores — forward-facing, intentional, instructive. Its spindles are executable sequences, not descriptions or histories. Recipes, kernels, seeds, and instruction sets are operational blocks. Where a rendition block decomposes knowledge and a living block accumulates experience, an operational block directs action.",
      "6": "The floor is shared not only across one block's growth but across different blocks. Because the floor is pscale 0 for every block, it is the one coordinate any two blocks hold in common, without negotiation. Relating blocks therefore indexes by pscale, never by walk depth: walk depth is local to a single block and pairs positions that do not correspond, while pscale lays blocks against the floor as a common plane — the deeper block's coarser context sitting above and finer detail below. Aligning two addresses pads the shallower floor with leading zeros to the wider, which is supernesting it up to the common floor. The operation has the dot product's shape, the floor its contraction axis. The canonical treatment is in bsp-mcp's sunstone:5.6 (composition by floor) and whetstone:7 (the bsp-floor operation)."
    },
    "5": "Position is semantic. The same content placed at different structural positions activates different meaning. An item as a peer of another item (flat sibling) retains its full independent polysemy — 'table' alongside 'room' can mean any table. The same item nested inside the other (child of its branch) has its meaning constrained by the parent — 'table' inside 'room' means the table in that room, and its expansions follow: contents rather than properties, function rather than material. Rearranging content between flat and nested positions is a compositional act with semantic consequences, not a storage choice. This is why block authoring matters: the same words, differently arranged, produce different spindles, different expansions, and different meaning-spaces."
  },
  "2": {
    "_": "BSP (Block-Spindle-Point) is a single function that reads any pscale block: bsp(block, number, mode?). It splits the number into individual digits and walks the tree one digit at a time, collecting the underscore text at each node it passes through — the result is a spindle, a chain from broad context to specific detail that delivers orientation and content simultaneously.",
    "1": {
      "_": "The walk is the core operation from which every mode derives. Given a number with at most one decimal point, first check the block's floor depth. If the address has fewer digits than the floor, pad with leading zeros to match — this places the address at the correct depth in the underscore chain. Then strip trailing zeros — they are floor-width notation, not walk instructions. Then strip the decimal point. The result is a sequence of digit characters to walk: start at the root object, and for each digit step into the child at that key — digit 0 maps to underscore, digits 1–9 map to their respective keys. At each step where the current node is an object, collect its underscore value into the spindle chain. Example: address 83 in a floor-3 block pads to 083, no trailing zeros to strip, walks digit 0 (underscore), 8, 3. Address 100 in floor 3 is already three digits, strip trailing zeros to get 1, walk digit 1 only.",
      "1": {
        "_": "The walk produces a sequence of underscore texts ordered from broadest (root) to most specific (terminal node). This sequence is the spindle — the default and most important return value, because it delivers not just the target content but every layer of context above it that makes the content meaningful.",
        "1": "Example: bsp(block, 2.1) walks digit 2 then digit 1. Collects: root._ (what the block is), then root[2]._ (what section 2 is about), then root[2][1] content (the specific detail). Three levels of meaning in one call — broad context narrowing to fine detail.",
        "2": "Example: bsp(block, 1.131) walks digits 1, 1, 3, 1 and collects five layers of underscore text — the root plus four levels of progressive narrowing. Longer numbers produce deeper spindles. The tree does not change; the number decides how deep you go.",
        "3": "A walk that reaches a digit with no corresponding key has hit the creative frontier — nothing exists there yet. This is not an error but information about where the block has capacity to grow. Return the spindle collected so far with a marker indicating the frontier."
      },
      "2": "If a node is a string rather than an object, it is a leaf — collect the string as the final value and terminate the walk. Any remaining digits in the number address nothing beyond this leaf. The spindle is complete at this point regardless of how many digits remain unwalked.",
      "3": "Digit 0 always maps to the underscore — stepping into the zero-position child rather than a numbered branch. Address 1.02 walks digit 1, then digit 0 (into that node's underscore), then digit 2 (into the underscore's second child). Every zero in an address is a navigation step into an underscore at that depth."
    },
    "2": {
      "_": "Spindle mode is the default — it returns the collected chain of underscore texts from root through each walked level to the terminal node. Every spindle includes the root, so you always know what block you are in. The spindle is what you hand to a mind: not just the detail you asked for, but the context that makes it meaningful. Context is structural, not optional.",
      "1": "Spindle quality equals block quality. If a spindle produces fragments instead of coherent narrowing — if the underscore text at any level is a heading rather than a thought — the block needs better content, not a better query. When every node carries substantive content, every spindle produces a coherent thread. When nodes are thin or cryptic, spindles deliver noise.",
      "2": "A bare-zero call — bsp(block, 0) — walks digit 0 into the root underscore. If the underscore is a string, it returns that string as the single cheapest orientation read. If the underscore has been subnested, it enters the zero-position interior. Start here when encountering any new block before committing to deeper navigation."
    },
    "3": {
      "_": "Ring mode — bsp(block, number, 'ring') — returns the siblings at the terminal point of the walk, showing what else exists at this depth beside the node the spindle would have landed on. Ring provides local lateral breadth: it answers 'what are the alternatives here?' without the vertical context of the spindle chain.",
      "1": "Ring complements spindle. After a spindle lands you somewhere and you sense there are alternatives — other options, sibling concerns, parallel facets — ring shows them. The spindle gives vertical context; ring gives horizontal context at the landing point.",
      "2": "Example: bsp(block, 1.2, 'ring') where the block has digits 1, 2, 3 at that level returns the content at sibling keys 1 and 3, excluding the walked-to key 2. You see the alternatives without re-reading the context you already have."
    },
    "4": {
      "_": "Dir mode — bsp(block, 'dir') — returns the entire tree: every branch, every depth, every piece of content the block contains. This is the most expensive mode in tokens but the most complete in coverage. Use it to survey a block you have not seen before, to explore structure freely, or to inspect full content during editorial work.",
      "1": "Dir-subtree — bsp(block, number, 'dir') — returns everything below where a spindle would land: all children and descendants of the target node, without the spindle chain above it. This is the editorial mode for inspecting a branch's full internal structure."
    },
    "5": "Point mode — bsp(block, number, pscale, 'point') — returns just one node at a specific pscale depth without the spindle chain above or below it. The cheapest targeted mode: use it when you already have context and need one specific piece of content without paying for the full spindle.",
    "6": "Disc mode — bsp(block, null, depth, 'disc') — returns all nodes at a given depth across the entire block, a transversal slice through every branch at a chosen scale. Where ring shows breadth at one point, disc shows breadth at one scale across the whole tree. Disc is the dashboard primitive for blocks where depth encodes a meaningful dimension like temporal frequency.",
    "7": {
      "_": "Navigation practice is the discipline of choosing which mode to reach for and when. The core principle is aperture-first: start broad with a bare-zero or dir call, narrow deliberately with spindles, and check breadth with ring when the depth feels incomplete.",
      "1": "Every BSP call has a token cost proportional to what it returns. A dir loads the full tree; a deep spindle loads every level it passes through; a ring loads all siblings. The trade between resolution and context budget is the fundamental navigation decision.",
      "2": "Named compounds combine primitives: a bulb is a spindle plus a ring at the landing point (vertical context plus lateral breadth in two calls). A dashboard is a disc on a temporal block at a given pscale. These are recipes, not additional modes — they emerge from practice."
    }
  },
  "3": {
    "_": "To operate on any pscale block, implement a walk function that accepts a block (a JSON object using underscore and digit keys), a number (the BSP address as a string or float), and optionally a mode string. The walk is the single core algorithm — every mode is a variation of how the walk's results are collected, filtered, or redirected. A complete implementation needs the walk, floor detection, and mode dispatch.",
    "1": {
      "_": "The core walk algorithm: parse the input number by removing the decimal point if present (it is a readability marker for the floor boundary) and splitting the resulting string into individual digit characters. Initialise an empty spindle array and set the current node to the block's root object. For each digit in the sequence: if digit is '0', look up key '_'; otherwise look up the key matching that digit character. If the key exists and the value is an object, collect its '_' value into the spindle array and set the current node to this object. If the value is a string, collect it as the final entry and terminate. If the key does not exist, mark the creative frontier and terminate.",
      "1": "Handle the root specially: before walking any digits, if the root object has a '_' key and its value is a string, collect it as the first spindle entry. The root underscore is always the first element in any spindle because it provides the block-level orientation that contextualises everything deeper. If the root underscore is an object — either from supernesting or because the block is a zero-position block — the walk enters it via digit 0 rather than collecting it as text.",
      "2": "For accumulation numbers (floor > 1), digits before the decimal walk the upper levels of the tree that were created by supernesting. The walk is identical in mechanics — the decimal point affects pscale labelling, not traversal. Addresses at any floor use the same walk: strip the decimal, walk the digits.",
      "3": "Return the spindle array as the default result. Each element is a string of underscore text at successive depths, ordered from root (index 0) to terminal (last index). The array length equals the number of tree levels successfully traversed plus one for the root."
    },
    "2": {
      "_": "Floor detection: start at the root object, follow the underscore chain — if root['_'] is a string, floor is 1. If root['_'] is an object, check whether it has its own ['_'] key and repeat. Count steps until you reach a string. That count is the floor depth. If the chain encounters an object with no underscore of its own, the floor is the count of underscore keys traversed to reach it — that object is a zero-position interior, not a supernested level. Run this once per block and cache the result, because the floor does not change unless the block supernests.",
      "1": "Use the floor to compute pscale positions: a node at tree depth d has pscale equal to (floor minus d minus 1), where depth 0 is the root level. This lets you label spindle entries with their pscale values and implement point mode (extract content at a specific pscale) and disc mode (collect all nodes at a specific depth).",
      "2": "For rendition blocks (floor 1), the computation simplifies: pscale 0 is the root, pscale -1 is depth 1, pscale -2 is depth 2, and so on. The minus sign indicates decomposition below the decimal point — finer detail than the block's root resolution."
    },
    "3": {
      "_": "Mode dispatch after the walk completes: spindle returns the collected array directly. Ring returns siblings of the terminal node — find the parent (the node one step before terminal in the walk), collect all its digit keys and their underscore texts, excluding the key that was walked to. Dir returns the full block tree as-is for complete survey, or if a number was provided, returns the subtree at the walk's terminal node. Point uses the floor calculation to extract content at a specific pscale depth. Disc ignores the number and recursively walks the entire tree collecting every node at the target depth.",
      "1": "Ring implementation: during the walk, track the parent node (the node before the last step). After the walk, iterate the parent's keys, skip '_' and the terminal digit, and collect the value at each remaining digit key — if the value is a string return it directly, if an object return its '_' value.",
      "2": "Disc implementation: write a recursive traversal starting at the root with depth counter 0. At each node, if the current depth equals the target depth, collect the node's content and its address (the path of digits walked to reach it). Otherwise, for each digit key 1–9 that exists, recurse with depth incremented by one. Return the flat collection of all matches."
    }
  },
  "4": {
    "_": "This block is a pscale JSON block describing pscale JSON blocks — its structure demonstrates every property it describes. Address 1 is the format described in the format. Address 2 is BSP navigation described in a BSP-navigable structure. Address 3 is the implementation specification that, when read as a spindle, provides enough operational detail for an LLM to build the function that would read this very spindle. The self-referential property is the verification mechanism: if a spindle through this block does not produce coherent, self-contextualising content, the block has failed at what it describes.",
    "1": "This block is a rendition block — 0+, floor 1. Every underscore describes its own group. Digits are editorial labels, not sequential entries. It will never supernest because it is a specification, not a living accumulator. Its structure is fixed by design: the format it describes is the format it uses, and changes to the specification change the example simultaneously.",
    "2": "Every reader of this block is simultaneously a user and an auditor. Reading the block exercises the format and navigation it teaches. A spindle that delivers incoherent narrowing is both a broken explanation and a structural failure visible in the same moment — the medium and the message cannot diverge because they are the same object."
  },
  "5": {
    "_": "Starstone — the pscale touchstone extended with nested underscore collection, the star operator, and the sign of the block. Version 3.0, April 2026. Authored by David Pinto with Claude Code and Claude.ai. Version 3 adds the four underscore forms (0+, +0, 0−, −0), the plus/minus sign of the block, the pscale shift, and the bud transition.",
    "1": "Pure form means this block contains only underscore keys, digit keys 1–9, and curly brackets — no tuning field, no tree wrapper, no skeleton, no mask. The floor is derived from the underscore chain depth. Any implementation that can walk this structure can walk any pure-form pscale block.",
    "2": "Designed to be included in an LLM context window as a self-unpacking operational specification: one block that teaches the format, demonstrates it, and provides enough implementation detail for the reader to build its own BSP tools. The companion blocks pscale-guidelines and pscale-design carry the fuller treatments — craft, philosophy, design ethos, and the deeper implications of semantic addressing for LLM cognition."
  },
  "6": {
    "_": "When an underscore is an object rather than a string, the BSP walk follows the nested underscore chain to find the semantic text — collecting the deepest string reachable through successive underscore keys. Digit children alongside nested underscores are hidden directory content, invisible to normal spindle collection. This nested chain is the structural basis for the zero-position interior and the star operator.",
    "1": {
      "_": "The collect-underscore algorithm: given a node, check its underscore. If the underscore is a string, return it. If the underscore is an object with its own underscore, recurse. If the underscore is an object with no underscore of its own, return null — this is a zero-position interior with no semantic text. The same chain-following logic used for floor detection applies at every node during spindle collection.",
      "1": "In a floor-3 spatial block, a building node at depth 2 has its underscore subnested: the underscore object contains both its own underscore string (the building name) and digit children (hidden content about the building). The walk collects the building name for the spindle. The digit children are accessible only via digit 0 — address 110.1 walks into the building underscore and reaches the first hidden entry.",
      "2": "The number of underscore nesting levels at any node equals floor minus node depth. A neighbourhood at depth 1 in a floor-3 block needs two levels of nesting. A building at depth 2 needs one level. A room at the floor (depth 3) needs none — its underscore is already a string. The structure self-calibrates: deeper nodes need less nesting to place hidden content at consistent pscale positions."
    },
    "2": "Backward compatibility: all existing blocks have string underscores at every node. The nested chain only activates when an underscore is an object, which no current block uses. The change to the walk algorithm is additive — existing spindles are unchanged.",
    "3": "The hidden directory at any node is the set of digit children found alongside the nested underscore chain. These children sit at the zero position — between the node and its semantic text. They are structurally present but navigationally hidden: only a deliberate digit-0 walk or a star query reveals them."
  },
  "7": {
    "_": "The star operator composes BSP walks across block boundaries. Walk a block to an address, enter the hidden directory at that point, and follow what you find — inline content, an embedded block, or a block reference that names another loadable block. Star is function composition for meaning-structures: the output of one walk becomes the entry point of the next.",
    "1": {
      "_": "Star query — bsp(block, address, star) — returns the hidden directory contents at the terminal node of the walk. The result includes the semantic text at that node (the underscore chain string) and the digit children of the hidden directory. If no hidden directory exists, the result is null.",
      "1": "Hidden directory contents are one of three kinds: inline text (a string value returned as-is), a block reference (a string matching a loadable block name — the caller loads and navigates), or an inline block (a pscale object embedded directly, navigable without loading). The star operator returns whatever it finds. The caller interprets.",
      "2": "Star composes with all existing modes. After entering a hidden directory, the content found there can be navigated with spindle, ring, dir, point, or disc. Star is not a replacement for existing modes — it is orthogonal, adding a dimension of cross-block navigation to the existing within-block navigation."
    },
    "2": {
      "_": "Block references enable wiring between blocks. A spatial block carries identity references in its hidden directories. An identity block carries concern references. A concern block carries cooking references. The wiring is data, not code — no kernel logic decides which blocks connect. The hidden directories ARE the connections.",
      "1": "Self-reference: a block can name itself in its own hidden directory. The walk enters the hidden directory, finds the block name, loads the same block, and re-enters. This is structural recursion — not implemented in code but encoded in data. The block does not contain a recursive program; it IS a recursive structure.",
      "2": "A ring of blocks referencing each other through star creates a control loop. Each block is both perceiver and reference-setter. 0− underscores (forward-facing, intentional) serve as reference signals. Digit children serve as perceptual signals. The gap between them is the error. The star operator is the feedback path that connects one block to the next in the loop. The loop terminates when the error is zero — when the digits fulfil what the underscore states."
    },
    "3": "Star reduces the kernel to a walker. The kernel does not decide which blocks connect, which concerns fire, or which recipes resolve them. It walks. If it hits a star reference, it follows. If the walk terminates, the process rests. If the walk loops, the process is alive. The data is the program. The kernel is the electricity."
  },
  "8": {
    "_": "The sign of the block — plus or minus — is a property of the whole block determined by use, not stored in the JSON. Every address in a block inherits its sign regardless of internal pscale. Plus means the content is fixed and settled — shell. Minus means the content is mutable and active — process. The JSON is structurally identical either way. An LLM reads the content and infers the sign. Code observes mutations and infers the sign. The block itself is just a block.",
    "1": {
      "_": "A plus block is shell. Its semantic text is final. A history log, a frozen specification, a completed rendition. The present tense in a plus block is a leading point — zero-dimensional, a single location at any given depth where the next entry will land. Everything behind it is settled. The creative frontier is the only place where the block's truth is unresolved.",
      "1": "Plus blocks map to stateless infrastructure. Fetch, respond, forget. The content IS the state, fully resolved. No subscription needed, no mutation expected. A BSP walk into a plus block returns the same result every time.",
      "2": "A plus block that is still growing has exactly one partially completed bracket at any given depth level. That singular location is the growth point — the zero-dimensional present of a settled structure."
    },
    "2": {
      "_": "A minus block is process. Its semantic text is contingent and may be rewritten. A live purpose, an active concern, a relationship in progress. The present tense in a minus block is a leading edge — any location can be rewritten, the present is distributed across the entire block. Higher-dimensional. More degrees of freedom, more possible states.",
      "1": "Minus blocks map to stateful infrastructure. Subscribe, maintain, update. The meaning depends on the ongoing process, not just the snapshot. A BSP walk into a minus block may return different results at different moments because the content is being written.",
      "2": "The four underscore forms operate in both signs but with different character. A 0+ underscore in a plus block is permanently true. A 0+ underscore in a minus block is provisionally true — true now, subject to revision. The form describes the direction of the underscore's relationship. The sign describes whether that relationship is settled or live."
    },
    "3": "A minus block does not become a plus block. When content in a minus block is resolved — a purpose achieved, a concern closed — the semantic moves: it is recorded into a plus block. The purpose block remains minus, moving on to the next purpose. The history block remains plus, gaining a new settled entry. The transition is semantic content passing from a minus block to a plus block. This passage is the PCT evaluation — the moment where error between reference signal and perception reaches zero, and the resolved content transfers to the settled record.",
    "4": "The sign is the structural equivalent of the shelf metaphor: plus is solid, minus is liquid. Content solidifies by moving from a minus block to a plus block — not by the block itself changing state. The minus block is the workspace. The plus block is the archive. The passage between them is the act of completion."
  }
}