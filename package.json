{
  "name": "pscale-mcp-server",
  "version": "0.3.0",
  "description": "MCP server for pscale block operations — structured memory for any LLM agent",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc && cp -r src/public dist/public",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@supabase/supabase-js": "^2.89.0",
    "@vercel/node": "^5.7.3",
    "hash-wasm": "^4.12.0",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
